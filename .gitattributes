# Nothing here is Git-LFS any more: build packages are published as GitHub
# Release assets, not committed. (Older history still contains LFS pointers for
# the previous project's builds/*.zip — that history is left untouched.)
* text=auto eol=lf
